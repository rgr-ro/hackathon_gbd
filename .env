## Environment variables for docker-compose
# PostgreSQL credentials and database name
POSTGRES_USER=myuser
POSTGRES_PASSWORD=mypassword
POSTGRES_DB=mydb

# Host port used to publish the Postgres container (host:container)
# Change if you need to avoid conflicts on the host
DB_HOST_PORT=5432

# pgAdmin credentials and host port
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin
PGADMIN_HOST_PORT=8080

# Add other app-specific variables below as needed
